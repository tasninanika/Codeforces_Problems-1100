#include<bits/stdc++.h>
using namespace std;

int main(){
    const int MAXN = 100000;

    vector<int> binDec;
    queue<int> q;
    q.push(1);

    while(!q.empty()){
        int x = q.front(); q.pop();
    }

    return 0;
}
